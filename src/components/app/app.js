import React from 'react';

import MainPage from '../main_page/main-page';

const App = () => {
  return (
    <MainPage gameTime={7} errorCount={4} clickHandler={()=>{}}/>
  );
};

export default App;
